<Page
    x:Class="Tillsammens.WindowsPhone.App.Pages.MapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tillsammens.WindowsPhone.App.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Map, Source={StaticResource Locator}}">

    <Grid>
        <maps:MapControl Name="MapControl" Loaded="MapControl_Loaded" >
            <Ellipse Height="80"  Width="80"  maps:MapControl.Location="{Binding Location}" >
                <Ellipse.Fill>
                    <ImageBrush Stretch="UniformToFill" ImageSource="{Binding Friend.PhotoUri}"/>
                </Ellipse.Fill>
            </Ellipse>
        </maps:MapControl>
    </Grid>
</Page>
